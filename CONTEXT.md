# Text-SQL 问答

用户用自然语言问数；主 Agent 规划后，要么只对话，要么走固定查数管线。本文件只定词，不定实现。

未设计 / 未接线（假数据与规格未定项）：`textsql-agent/docs/agents/undesigned.md`。

流程：`textsql-agent-flow.md`（附图 `textsql-agent-flow.html`）。B3 提示词：`textsql-rewrite-prompt.md`。

## Language

**主 Agent**：
面对用户的规划者和说话者。不写 SQL，不查库。要查数时只调一次查数工具。记忆就是会话里的用户原话、自己说过的话、查数工具回填；联网搜索开启后还记搜索回填。不另做记忆模块。
_Avoid_: 网页版裸 LLM、查库大模型、Vanna、DeepSeek Harness（那是运行时，不是这个角色）；槽位状态机

**一轮**：
用户的一句输入。主 Agent 对这一句只规划一次，只进入环 A 或环 B 其中一条。
_Avoid_: 同一句用户话里先澄清再查数；把一次 ReAct 多跳算作一轮

**环 A**：
本轮不查库的正常对话。不调查数工具，不调用小模型、Vanna RAG、查库大模型。联网搜索默认关闭；开启后本环才可调。
_Avoid_: 在环 A 里编造库中的精确数；把网页上的数字说成库里查到的数；默认关闭时仍去搜网

**联网搜索**：
为主 Agent 设计的、在环 A 里查公开网页资料的工具。默认关闭、不挂载。查的是网上的说明，不是库里的数。
_Avoid_: 给查库大模型联网；用搜索代替查数工具；同一句用户话里又搜又查库；当成已经上线的能力

**查数工具**：
主 Agent 用来启动环 B 工作流的那一个工具。默认开启。入参是当前问题。内部跑完 B3→B6 再回填。一句用户话最多调一次。
_Avoid_: 把 B3、B4、B5 拆成主 Agent 的多个工具；一句调两次；主 Agent 在工具内部插步

**环 B**：
查数工具内部的固定顺序：小模型改写 → Vanna 内 RAG → 查库大模型在一次查库对话里调用只读查库工具 → 回填主 Agent。
_Avoid_: 把环 A 和环 B 画成同一张分叉图；打乱 B3→B6 的顺序；让主 Agent 直接调小模型或查库大模型

**当前问题**：
主 Agent 整理后交给查数工具的、单独拿出来也能查的那一句。用户原话只进主 Agent。首轮没有上次 SQL、也没有查询摘要，小模型只吃这一句。
_Avoid_: 用户原话、口语指代（「就按第一个」）；首轮硬要上次 SQL

**小模型**：
无跨轮记忆的改写模型。不查库。输入：固定前缀、上次 SQL（可空）、查询摘要（可空）、当前问题。输出：改写问题 + 需求说明两段。
_Avoid_: 给小模型近几轮对话；把小模型当规划者；说小模型跨轮「维护」状态；给小模型加第五段

**改写问题**：
小模型输出的、拿去走 Vanna RAG 的那一句。
_Avoid_: 用户原话、查询摘要

**上次 SQL**：
最近一次查库成功的 SQL，包括结果为 0 行。与查询摘要成对落盘；仅查库成功时一起覆盖，否则两个槽都不动。
_Avoid_: 失败 SQL；把 0 行当失败而不覆盖；摘要已更新但 SQL 未更新

**查询摘要**：
小模型改写时产出的自然语言说明，段名「需求说明」。怎么写由小模型自己决定；没把握的就照实说不确定，不编。程序只原样存取，不解析它的内容、也不按内容做分支（ADR 0002）。与上次 SQL 成对落盘，只给下一轮小模型看。
_Avoid_: 主 Agent 写摘要；小模型自己存摘要；程序按摘要内容做判断；写行数、报错、结果；与上次 SQL 单独落盘

**查库成功**：
只读 SQL 执行未报错。包括 0 行。
_Avoid_: 把 0 行当失败；主 Agent「觉得对」才算成功

**没查到**：
查库成功且结果为 0 行。对用户如实说没查到；槽位仍按查库成功覆盖。
_Avoid_: 把没查到当成 SQL 失败；系统自己改条件再查一次

**查库大模型**：
在一次查库对话里生成 SQL，并调用只读查库工具执行的模型。对话内能看见工具返回的报错、超时并改 SQL。不是数据库在调工具。
_Avoid_: Vanna 去查库；给它其它工具或联网搜索；查库大模型再检索一遍；查库大模型跟用户说话

**只读查库工具**：
查库大模型唯一能调的工具。只执行 SELECT。次数上限和超时由程序执行，超限或超时当作这一次工具失败返回给模型。
_Avoid_: 增删改、改表、其它工具、管理员账号

**查库对话**：
查库大模型针对这一次改写问题 + 这一次 RAG 材料的全新会话。新的 RAG 材料到来，或离开 B5，就丢弃这次对话。
_Avoid_: 计时器；把上一份 RAG 的对话接着用；用次数上限去过期上次 SQL；小模型记忆；主 Agent 记忆

**Vanna RAG**：
Vanna 框架内对 DDL、文档/口径、问题-SQL 样例的一次检索。query 是改写问题。
_Avoid_: 外挂第二套检索；查库大模型二次检索
