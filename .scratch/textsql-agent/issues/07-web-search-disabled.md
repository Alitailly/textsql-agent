# 07: 联网搜索契约在，默认不挂

**What to build:** 联网搜索按已定契约实现，但开关默认关、不挂到主 Agent。任意一轮该工具调用次数为 0。本工单不打开开关。

**Blocked by:** 01 环 A 一轮能说话

**Status:** done

- [x] 工具契约已实现：入参一句搜索词；成功最多 5 条（标题、网址、摘要约 300 字）；失败返回错误原文
- [x] 默认关闭：运行中的主 Agent 工具列表里没有联网搜索，一轮测试中调用次数为 0
- [x] 开启后的保险丝已写进实现（每句环 A 最多 3 次、单次 15 秒），但默认路径走不到
- [x] 即使以后打开，也不会给查库大模型，也不会在同一句里又搜又调查数工具——本工单用测试或护栏锁住「默认关」即可
