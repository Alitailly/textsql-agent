# 03: 当前问题消指代

**What to build:** 口语指代和「先聊后查」时，查数工具拿到的是单独能查的当前问题。用户原话只留在主 Agent 会话里。

**Blocked by:** 02 查数工具一次（内部全替身）走通

**Status:** done

- [x] 主 Agent 先给出可选项，用户说「就按你说的第一个」时，查数工具入参是完整可查的当前问题，不是这句原话本身
- [x] 「先告诉我销售额口径」走环 A（查数工具 0 次）；下一句「查吧」才调查数工具 1 次，且当前问题带上已澄清的口径
- [x] 小模型侧仍然看不到近几轮对话，只看到当前问题（加可空的上次 SQL / 查询摘要）
