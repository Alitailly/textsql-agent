# 05: Vanna RAG 只检索一次

**What to build:** 改写问题作为 query，对照 DDL、文档/口径、问题-SQL 各一路，命中材料交给查库对话。不会再检索第二遍。

**Blocked by:** 04 小模型按契约改写

**Status:** done

- [x] 查数工具内部对改写问题做一次三路检索，查库大模型吃到的是这一次命中材料
- [x] 默认 generate_sql 全链不会被走一遍，因而没有第二次检索
- [x] 检索不执行 SQL；环 A 仍然调查数工具 0 次
