# 小模型改写 · 简单提示词

词以 `CONTEXT.md` 为准。整条环见 `textsql-agent-flow.md`。业务词不进 `src/`（ADR 0002）。

无记忆。不查库。不输出 SQL。只根据这一次输入改写。

下面例子用「销售额 / 按城市 / 含税」当示踪弹，用来锁住两段契约，不是把小模型设计成只懂这一门业务。口径写成「含税/未税」是举例，不是通用口径枚举。

## 固定前缀

```text
你是问句改写器。只根据下面三块输入，产出「改写问题」和「需求说明」两段。不要解释，不要 SQL，不要回答这个数是多少。

规则：
1. 上次SQL、查询摘要为空：只根据当前问题改写。
2. 上次SQL或查询摘要有值：把当前问题当成对上一轮的修改（加条件、改维度、改口径、取消某过滤），改写成一句单独能查的完整问句。
3. 当前问题和摘要里都没有的条件不要编。拿不准就照实说不确定。
4. 需求说明写成一行自然语言，说清这次要什么、跟上一轮比改了什么。
5. 严格按输出格式，一行一段。

输出格式：
改写问题：<一句完整中文问句>
需求说明：<一行自然语言说明>
```

第二段是自由文本：程序只原样存起来给下一轮看，不解析、不按它做分支（ADR 0002）。所以写法由小模型自己定。

程序怎么收这两段：「改写问题」只认标签后那一行；「需求说明」从标签起，一直取到输出末尾，原样存。所以规则里的「不要 SQL、不要解释」必须自己守住——解析层不替你删。

## 每次再拼上

```text
上次SQL：
{上次SQL 或 空}

查询摘要：
{上一次的需求说明 或 空}

当前问题：
{当前问题}
```

## 首轮例子

输入：上次 SQL、摘要都空；当前问题 = `2026年8月销售额是多少`

```text
改写问题：查询2026年8月的销售额
需求说明：要2026年8月的销售额合计，口径未说明。
```

## 续问例子

上次摘要已有「口径未说明，2026年8月的销售额」；当前问题 = `那按城市呢`

```text
改写问题：查询2026年8月按城市分组的销售额
需求说明：沿用2026年8月，改成按城市分组，口径仍未说明。
```

## 0 行后续问例子

上次说明是「按城市分组，某市查不到」、结果是 0 行（摘要仍会落盘）；当前问题 = `别按城市了`

```text
改写问题：查询2026年8月的销售额
需求说明：沿用2026年8月，去掉按城市分组。
```

## 认不出契约时

「改写问题」这一段认不出来，程序退回当前问题原句，并把模型原文留在改写输出里给人看；「需求说明」认不出来（没有这个标签）就是空。这是可见的软降级，不是静默兜底：退回只少一层改写，不丢条件。
