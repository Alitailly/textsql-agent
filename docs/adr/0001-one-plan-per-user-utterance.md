# 一句用户话只规划一次

主 Agent 按 ReAct 思考，但每一句用户输入只做一次环 A / 环 B 分流。要查数就调查数工具一次；不查数就不调。需要先澄清再查数时，拆成两句用户话。联网搜索默认关闭；开启后，「先上网查公开说明再查库」同样拆成两句。同一句里不又搜网页又调查数工具。

查数工具内部的查库对话可以多次只读跑 SQL，那是 B5 小环，不算主 Agent 再调一次查数工具。联网搜索开启后，环 A 里多次搜索是聊天工具，也不算调查数工具。

词和流程以仓库根目录 `CONTEXT.md`、`textsql-agent-flow.md` 为准。
