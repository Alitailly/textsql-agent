# Text-SQL 问答系统 · 设计说明

词以 `CONTEXT.md` 为准。B3 提示词以 `textsql-rewrite-prompt.md` 为准。附图：`textsql-agent-flow.html`。

本文件写的是**设计意图**。哪些已经接线、哪些还是替身，见 `README.md` 的「哪些是真接通的、哪些还是替身」一节；实现边界见 `.scratch/textsql-agent/START-HERE.md`。**不要拿设计当现状。**

---

## 〇、出发点：主 Agent 是一个决策循环

主 Agent 不是「每句话只规划一次」的二选一开关，而是一个 **ReAct 循环**：

```text
思考（Reason）→ 行动（Act）→ 观察（Observe）→ 再思考 → ……
```

每一圈它只问一个问题：**「手上这些材料，够不够回答用户？」**

- **够了** → 行动是「说话」，本轮结束。
- **不够** → 行动是「调一个工具」，拿到回填，回到思考，再问一遍。

一轮（用户的一句话）里可以转好几圈，中途可以换工具。转几圈由程序兜底，不靠模型自觉。

主 Agent 手上只有三个动作：

| 动作 | 干什么 | 什么时候用 |
|---|---|---|
| **说话** | 组织人话回复用户 | 认为材料已经够回答 |
| **调查数工具** | 启动环 B，取库里的确切事实 | 需要库里的锚点（是谁、多少粉、哪个邮箱） |
| **联网搜索** | 查公开网页，补时效性内容 | **拿到库内容后**，判断「这样回复还不够 / 不对」 |

**边界：** 主 Agent 不写 SQL、不查库、**也不参与改写问题**。它交给查数工具的只有「当前问题」这一句；改写完全由工具内部的小模型负责。

---

## 一、一轮怎么走

```text
用户说话
    ↓
主 Agent 决策循环（思考 → 行动 → 观察 → 再思考）
    │
    ├─ 够回答 ─→ 说话 ──────────────→ 回复用户（本轮结束）
    │
    ├─ 要库里的确切事实 ─→ 调查数工具（每轮最多 1 次）
    │        │  工具内部跑环 B：B3 改写 → B4 检索 → B5 查库对话 → B6 回填
    │        ↓  观察回填（有行 / 没查到 / 报错）
    │        └────────────────────→ 回到「思考」
    │
    └─ 拿到库内容后判断不够 ─→ 联网搜索（默认关闭，每轮最多 3 次）
             ↓  观察搜索结果
             └────────────────────→ 回到「思考」
```

两条硬约束由程序执行，不靠模型自觉：

- **查数工具每一轮最多调 1 次。** 第二次照拒。
- **联网搜索开启后每一轮最多 3 次**，单次超时 15 秒；3 次用尽仍失败就停止搜索、直接说话。

被拒的工具**不是打断整轮**，而是从主 Agent 能看见的工具集里摘掉，循环继续——让它用手上已有的回填写。宁可它明说「这一轮没成」，也不要静默回一个空串。

---

## 二、环 A · 正常对话（不查数）

没有小模型、没有 Vanna RAG、没有查库大模型。主 Agent 直接用会话记忆说话。联网搜索默认关闭；开启后本环也可先搜再说话。

```text
          ┌─────────────────────────┐
          │                         │
          ▼                         │
    A1 用户说话                      │
          │                         │
          ▼                         │
    A2 主 Agent                     │
       会话记忆 + 大模型说话          │
       （联网搜索开启后，可先搜再说话）  │
          │                         │
          ▼                         │
    A3 回复用户                      │
          │                         │
          └─────────────────────────┘
               等下一句
```

| 模块 | 在环 A 干什么 |
|------|----------------|
| 规划 | 本轮不查数 |
| 记忆 | 用户原话 + 已有会话 + 本轮这句话 |
| 工具 | 不调查数工具。默认也不调联网搜索。不调用小模型 / Vanna RAG / 查库大模型 |
| 大模型 | 根据对话上下文组织回复 |

```text
[系统角色：对人说话，库里的精确数只来自查库回填]
[会话记忆]
[用户本轮这句话]
        → 主 Agent
        → 回复用户
```

完成：已经用人话回复，且本轮没有调查数工具。

---

## 三、查数工具（默认开启）

环 B 整条工作流封装成主 Agent 的这一个工具。默认挂载。主 Agent 不拆开调小模型、Vanna RAG、查库大模型。程序槽里的上次 SQL、查询摘要由工具内部去读，不经主 Agent 传入。

```text
名称：查数工具
调用方：仅主 Agent
开关：默认开启，挂载

入：
  当前问题：单独拿出来也能查的一句

出（有行）：
  SQL + 截断后的结果（最多 50 行或约 4000 字）

出（0 行）：
  没查到（槽仍成对覆盖）

出（工具失败用尽）：
  最后 SQL + 报错（两槽不动）

副作用：
  查库成功时程序成对覆盖上次 SQL + 需求说明
  失败用尽时本轮摘要扔掉

内部（主 Agent 不可插步）：
  B3 小模型改写 → B4 Vanna RAG 一次 → B5 查库对话 → B6 回填

约束：
  同一句用户话最多调 1 次
```

完成：工具已返回上述三选一，且内部查库对话已丢掉。然后主 Agent 回到循环，决定是说话还是再要别的材料。

---

## 四、环 B · 查数工具内部的工作流

B5 查库对话是套在环 B 里的小环：同一份 RAG 材料、只读跑 SQL。新的 RAG 到来，或离开 B5，就丢掉这次查库对话。

```text
    B2 主 Agent 写出当前问题，调查数工具一次
          │
          ▼
    B3 小模型改写（无记忆）
       输出：改写问题 + 需求说明
          │
          ▼
    B4 Vanna 内 RAG（三路对照一次）
          │
          ▼
    B5 查库对话（全新）
       只调只读查库工具
       报错/超时 → 对话内改 SQL 再跑
       有行或 0 行 → 停
          │
          ▼
    B6 结果回填主 Agent
       查库成功才成对写入 SQL + 摘要
```

### B2 当前问题

用户原话只进主 Agent。交给小模型的是单独拿出来也能查的一句。首轮没有上次 SQL、也没有查询摘要。

**主 Agent 到此为止。** 它不做改写——把「当前问题」递进去就结束了，剩下全是工具内部的事。

完成：当前问题里没有「就按第一个」这类指代。

### B3 小模型

**改写全部在这一步。** 无记忆。不查库。不输出 SQL。输入和输出格式见 `textsql-rewrite-prompt.md`。

```text
[固定前缀]
[上次 SQL]      ← 没有就空
[查询摘要]      ← 自由文本；没有就空
[当前问题]
→ 改写问题 + 需求说明
```

改写问题直接进 B4。需求说明先拿在手里，等 B6 和上次 SQL 成对落盘。程序只原样存这段说明，不解析它的内容、也不按它做分支（ADR 0002）。

完成：输出恰好两段（改写问题 + 需求说明），没有解释、没有 SQL。

### B4 Vanna 框架内的 RAG

query 是改写问题。对照已 train 的三路：DDL、文档/口径、问题-SQL 样例。只检索，不执行 SQL。

```text
改写问题
    → Vanna RAG（对照 DDL / 文档口径 / 问题-SQL 样例）
    → 这一次命中材料
    → 交给 B5（不再搜第二遍）
```

完成：查库大模型拿到的是这一次三路命中，不是全库表，也不是第二次检索。

### B5 查库对话

Vanna 是框架。查库的是查库大模型。它在一次全新对话里生成 SQL，并调用**只读查库工具**。其它工具一概不给。不能问用户，不能调小模型，不能再走 RAG。

```text
[固定前缀]        只读、不准编表、要 LIMIT
[改写问题]
[这一次 RAG 命中材料]
→ 调只读查库工具
```

| 工具返回 | 接着做什么 |
|----------|------------|
| SQL 报错或超时 | 算一次调用失败，对话内改 SQL 再跑 |
| 有行 | 查库成功，离开 B5 |
| 0 行 | 查库成功（没查到），离开 B5 |

保险丝（程序执行，不是计时器，也不过期上次 SQL）：

- 每次查库对话最多 **5** 次只读调用
- 单次执行超时 **30** 秒，算一次调用失败并计入这 5 次
- 5 次用尽仍报错或超时：离开 B5，两槽不动

离开 B5：丢掉这次查库对话。主 Agent 会话不清。

完成：已经离开 B5，且这次对话已丢弃。

### B6 回填后

回填是两路：给人说话的是截断结果 / 没查到 / 报错；给下一轮小模型的是程序槽。

| 情况 | 给主 Agent | 上次 SQL + 查询摘要 |
|------|------------|---------------------|
| 有行 | SQL + 截断后的结果（最多 50 行或约 4000 字） | 成对覆盖 |
| 0 行 | 没查到 | 成对覆盖（0 行那条 SQL + 本轮需求说明） |
| 工具失败用尽 | 最后 SQL + 报错 | 旧值不动；本轮摘要扔掉 |

主 Agent 拿到回填后回到决策循环。**它可能就此说话，也可能判断「这些还不够」——那就去联网搜索再补一层。**

完成：回填已经交到主 Agent 手上；两个槽要么成对覆盖，要么都没动。

---

## 五、联网搜索（主 Agent 的决定，不在环 B 内）

**这是设计里的关键一点：联网搜索不是环 A 的专利，也不是环 B 的一个环节。**

它是主 Agent 自己的一个动作。典型情形正是本项目的价值主张所在：

```text
主 Agent 调查数工具 → 拿到库里的确切事实（谁、多少粉、什么类目、哪个邮箱）
        ↓
判断：光拿这些回复用户，不够 / 不对
        ↓
联网搜索，补充库外的时效性内容
        ↓
把「库里的锚点」和「网上的补充」合成一句回复
```

库给的是**不可幻觉的锚点**，搜索给的是**时效性**，主 Agent 给的是**合成**。这三样缺一样，就跟通用网页版没区别。

它**不封装进查数工具内部**——查数工具只负责把库里的东西拿回来（B3→B6），搜索是主 Agent 在工具之外自己决定的事。

```text
名称：联网搜索
调用方：仅主 Agent
开关：默认关闭，不挂载

入：
  查询：一句搜索词

出（成功）：
  最多 5 条
  每条：标题、网址、摘要（单条摘要截到约 300 字）

出（失败）：
  错误原文（超时、无结果、网络失败）
  主 Agent 对人说明搜不到，不编网页内容
```

保险丝：每一轮最多 **3** 次调用；单次超时 **15** 秒，算一次失败并计入这 3 次。3 次用尽仍失败则停止搜索、对人说话。

搜索回填进主 Agent 会话，**不写进上次 SQL / 查询摘要**。网页上的数字不当成库里查到的数。

完成：主 Agent 拿到搜索结果并回到决策循环；无论搜没搜到，回复里都不把网页数字说成库里的数。

---

## 六、两环对照

| | 环 A 正常对话 | 环 B 查数 |
|--|---------------|-----------|
| 何时进 | 本轮不需要查库 | 主 Agent 调查数工具一次 |
| 经过谁 | 只有主 Agent | 主 Agent 调一次查数工具（内部：B3 → B4 → B5 → B6）→ 回填主 Agent |
| 小模型 / Vanna RAG / 查库大模型 | 不出现 | 只出现在查数工具内部 |
| 查数工具 | 不调 | 调一次 |
| 联网搜索 | 主 Agent 可自行决定 | **不在环 B 内部**；查完库回到循环后，主 Agent 同样可自行决定 |
| 结束 | 回复用户，等下一句 | 回复用户，等下一句 |
| 主 Agent 记忆 | 会话 | 会话 |
| 小模型记忆 | — | 无跨轮记忆 |
| 查库大模型记忆 | — | 仅这一次查库对话；新 RAG 则全新 |

---

## 七、环 B 配置：只读库、Vanna RAG

### 1. 只读查库工具

程序提供给查库大模型的唯一工具。配的是连接和权限。

| 配什么 | 怎么定 |
|--------|--------|
| 地址 | host、端口、库名 |
| 账号 | 只读账号（只能 SELECT） |
| 执行 | 模型调工具，程序跑 SQL |
| 保险 | 禁写语句、表白名单、LIMIT、超时 30 秒、每段对话最多 5 次 |

```text
Vanna 配置
  ├── 查库大模型（生成 SQL，只调只读查库工具）
  └── 只读数据库连接
        host / port / database / user(只读) / password
```

生产用只读账号。报错原文返回给这段查库对话。

### 2. Vanna 自带 RAG

检索只用框架内 RAG。query 是改写问题。

| 字段 | 里面放什么 | 改写问题拿来对照什么 |
|------|------------|----------------------|
| **DDL / 表结构** | 建表语句、字段名、类型 | 这次可能用到哪些表、哪些列 |
| **文档 / 口径** | 指标含义、业务说明、同义词 | 「销售额」对应哪一列、怎么过滤 |
| **问题-SQL 样例** | 以前的完整问题 + 对应 SQL | 有没有相似问法可以参考 |

```text
改写问题
    → Vanna 内向量检索
        ├─ 对照「表结构」→ 命中相关 DDL
        ├─ 对照「文档口径」→ 命中相关说明
        └─ 对照「问题-SQL」→ 命中相似样例
    → 三路命中拼在一起
    → 交给查库对话（不再搜第二遍）
```

实现时走「改写句 → 框架 retrieve → 把 context 交给查库对话」。不要走默认 `generate_sql(问题)` 全链，那会再检索一次。

三类都要预先 train。缺哪一类，对照就会偏。

---

## 八、为什么这个循环能转起来：ReAct 思考模式

```text
思考（Reason）→ 行动（Act）→ 观察（Observe）→ 再思考 → ……
```

| ReAct | 在本系统里 |
|-------|------------|
| **观察** | 用户这句话；工具回填：查数结果、没查到、报错，或搜索结果 |
| **思考** | 主 Agent 自问：现在够不够回答用户？还缺什么？ |
| **行动** | 说话；或调查数工具一次；或联网搜索（默认关闭） |
| **再观察** | 工具回填或用户下一句 → 再思考 |

- **纯对话**：观察用户 → 思考「不用查库」→ 说话 → 等下一句
- **查数**：观察用户 → 思考「要库里的锚点」→ 写出当前问题 → 调查数工具一次 → 观察回填 → 判断够不够
- **查完还不够**：观察回填 → 思考「这些不足以回答」 → 联网搜索 → 观察搜索结果 → 合成回复

思考主体是主 Agent。查数工具是它启动环 B 的那一只手；联网搜索是它补时效性的另一只手。小模型、Vanna RAG、查库大模型在工具内部，主 Agent 不能单独调。查库大模型在 B5 里可以多跑几次只读 SQL，那是查库对话，不是主 Agent 再调一次查数工具。

---

## 九、已定数字

| 项 | 值 |
|----|----|
| 查数工具 | **默认开启**；每句最多 1 次 |
| 每段查库对话最多只读调用 | 5 |
| 单次 SQL 超时 | 30 秒 |
| 回填截断 | 50 行或约 4000 字 |
| 联网搜索 | **默认关闭**；主 Agent 在拿到库内容后按需调用 |
| 联网搜索开启后：每轮最多调用 | 3 |
| 联网搜索开启后：每次最多条数 / 单条摘要 | 5 条 / 约 300 字 |
| 联网搜索开启后：单次超时 | 15 秒 |
| 主 Agent 决策循环上限 | 3 + 3 = 6 |

表白名单具体表、Vanna 三类训练材料正文、多业务是否拆训练空间：落地时再填。
