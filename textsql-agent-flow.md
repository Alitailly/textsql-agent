# Text-SQL 问答系统 · 设计说明

词以 `CONTEXT.md` 为准。B3 提示词以 `textsql-rewrite-prompt.md` 为准。附图：`textsql-agent-flow.html`。

两套闭环分开画。

- **环 A：正常对话**（不查库。联网搜索已设计、默认关闭；开启前本环只说话）
- **环 B：查数对话**（小模型改写 → Vanna 内 RAG 一次 → 查库对话 → 回填）

**前提：** 主 Agent 按 ReAct（Reason + Act，口语里的 React 思考模式）转圈。不是 React 前端。主 Agent 不写 SQL、不查库。每一句用户话只规划一次，只进环 A 或环 B。同一句里不又搜网页又查库。

主 Agent 的记忆就是会话里的用户原话、自己说过的话、查数工具回填；联网搜索开启后还记搜索回填。不另做记忆模块。运行时若用 DeepSeek Harness：只挂「说话」和「查数工具」。联网搜索按契约实现，但**默认不挂载**。bash / 写文件关掉。

---

## 〇、每一轮怎么选环

```text
用户说话
    → 主 Agent 规划
          ├─ 不需要查数据库 → 走环 A（不调查数工具）
          └─ 需要查数据库   → 调查数工具一次（内部跑环 B）
走完任意一环，都回到「等下一句用户」
```

完成：本句已经进了且只进了一环，并且已经回复用户。调查数工具 = 进环 B；不调 = 环 A。

需要先把口径说清楚再查：本句走环 A，等用户下一句再调查数工具。联网搜索开启后，「先上网查公开说明再查库」同样拆成两句。

---

## 环 A · 正常对话（不查数）

没有小模型、没有 Vanna RAG、没有查库大模型。联网搜索默认关闭，本环只说话。开启后才允许在 A2 调该工具。

```text
          ┌─────────────────────────┐
          │                         │
          ▼                         │
    A1 用户说话                      │
          │                         │
          ▼                         │
    A2 主 Agent                     │
       规划已判定：不查数             │
       会话记忆 + 大模型说话          │
       （联网搜索开启后才可搜）        │
          │                         │
          ▼                         │
    A3 回复用户                      │
          │                         │
          └─────────────────────────┘
               等下一句
```

| 模块 | 在环 A 干什么 |
|------|----------------|
| 规划 | 本轮不查数 |
| 记忆 | 用户原话 + 已有会话 + 本轮这句话 |
| 工具 | 不调查数工具。默认也不调联网搜索。不调用小模型 / Vanna RAG / 查库大模型 |
| 大模型 | 根据对话上下文组织回复 |

```text
[系统角色：对人说话，库里的精确数只来自查库回填]
[会话记忆]
[用户本轮这句话]
        → 主 Agent
        → 回复用户
```

完成：已经用人话回复，且本轮没有调查数工具。默认关闭时本轮联网搜索次数为 0。

### 联网搜索工具（已设计，默认关闭）

实现时按这个契约写好，但开关默认关、运行时不挂到主 Agent。开启之前不要改环 A 的完成条件。开启之后才允许在环 A 里调用；环 B 始终不调。同一句仍不又搜又查。网页数字不当成库里的数。

```text
名称：联网搜索
调用方：仅主 Agent，且仅环 A
开关：默认关闭，不挂载

入：
  查询：一句搜索词

出（成功）：
  最多 5 条
  每条：标题、网址、摘要（单条摘要截到约 300 字）

出（失败）：
  错误原文（超时、无结果、网络失败）
  主 Agent 对人说明搜不到，不编网页内容
```

开启后的保险丝：每一句环 A 最多 **3** 次调用；单次超时 **15** 秒，算一次失败并计入这 3 次。3 次用尽仍失败则停止搜索、对人说话。搜索回填进主 Agent 会话，不写进上次 SQL / 查询摘要。

### 查数工具（默认开启）

环 B 整条工作流封装成主 Agent 的这一个工具。默认挂载。主 Agent 不拆开调小模型、Vanna RAG、查库大模型。程序槽里的上次 SQL、查询摘要由工具内部去读，不经主 Agent 传入。

```text
名称：查数工具
调用方：仅主 Agent
开关：默认开启，挂载

入：
  当前问题：单独拿出来也能查的一句

出（有行）：
  SQL + 截断后的结果（最多 50 行或约 4000 字）

出（0 行）：
  没查到（槽仍成对覆盖）

出（工具失败用尽）：
  最后 SQL + 报错（两槽不动）

副作用：
  查库成功时程序成对覆盖上次 SQL + 需求说明
  失败用尽时本轮摘要扔掉

内部（主 Agent 不可插步）：
  B3 小模型改写 → B4 Vanna RAG 一次 → B5 查库对话 → B6 回填

约束：
  同一句用户话最多调 1 次
  与联网搜索不同句（搜索开启后也一样）
```

完成：工具已返回上述三选一，且内部查库对话已丢掉。然后主 Agent 用人话回复用户。

---

## 环 B · 查数对话

查数工具内部的工作流。查完、回填完，主 Agent 再对人说话。  
B5 查库对话是套在环 B 里的小环：同一份 RAG 材料、只读跑 SQL。新的 RAG 到来，或离开 B5，就丢掉这次查库对话。

```text
          ┌──────────────────────────────────┐
          │                                  │
          ▼                                  │
    B1 用户说话                               │
          │                                  │
          ▼                                  │
    B2 主 Agent 规划已判定：要查数             │
       写出当前问题，调查数工具一次             │
          │                                  │
          ▼                                  │
    B3 小模型改写（无记忆）                    │
       输出：改写问题 + 需求说明                │
          │                                  │
          ▼                                  │
    B4 Vanna 内 RAG（三字段对照一次）          │
          │                                  │
          ▼                                  │
    B5 查库对话（全新）                        │
       只调只读查库工具                        │
       报错/超时 → 对话内改 SQL 再跑            │
       有行或 0 行 → 停                        │
          │                                  │
          ▼                                  │
    B6 结果回填主 Agent，回复用户              │
       查库成功才成对写入 SQL+摘要              │
          │                                  │
          └──────────────────────────────────┘
               等下一句
```

同一句用户话，查数工具最多调一次。0 行、结果难看、想再收窄：回填主 Agent 对人说完，等下一句。

### B2 当前问题

用户原话只进主 Agent。交给小模型的是单独拿出来也能查的一句。首轮没有上次 SQL、也没有查询摘要。

完成：当前问题里没有「就按第一个」这类指代。

### B3 小模型

无记忆。不查库。不输出 SQL。输入和输出格式见 `textsql-rewrite-prompt.md`。

```text
[固定前缀]
[上次 SQL]      ← 没有就空
[查询摘要]      ← 自由文本；没有就空
[当前问题]
→ 改写问题 + 需求说明
```

改写问题直接进 B4。需求说明先拿在手里，等 B6 和上次 SQL 成对落盘。
程序只原样存这段说明，不解析它的内容、也不按它做分支（ADR 0002）。

完成：输出恰好两段（改写问题 + 需求说明），没有解释、没有 SQL。

### B4 Vanna 框架内的 RAG

query 是改写问题。对照已 train 的三路：DDL、文档/口径、问题-SQL 样例。只检索，不执行 SQL。

```text
改写问题
    → Vanna RAG（对照 DDL / 文档口径 / 问题-SQL 样例）
    → 这一次命中材料
    → 交给 B5（不再搜第二遍）
```

完成：查库大模型拿到的是这一次三路命中，不是全库表，也不是第二次检索。

### B5 查库对话

Vanna 是框架。查库的是查库大模型。它在一次全新对话里生成 SQL，并调用**只读查库工具**。其它工具一概不给。不能问用户，不能调小模型，不能再走 RAG。

```text
[固定前缀]        只读、不准编表、要 LIMIT
[改写问题]
[这一次 RAG 命中材料]
→ 调只读查库工具
```

| 工具返回 | 接着做什么 |
|----------|------------|
| SQL 报错或超时 | 算一次调用失败，对话内改 SQL 再跑 |
| 有行 | 查库成功，离开 B5 |
| 0 行 | 查库成功（没查到），离开 B5 |

保险丝（程序执行，不是计时器，也不过期上次 SQL）：

- 每次查库对话最多 **5** 次只读调用
- 单次执行超时 **30** 秒，算一次调用失败并计入这 5 次
- 5 次用尽仍报错或超时：离开 B5，两槽不动

离开 B5：丢掉这次查库对话。主 Agent 会话不清。

完成：已经离开 B5，且这次对话已丢弃。

### B6 回填后

回填是两路：给人说话的是截断结果 / 没查到 / 报错；给下一轮小模型的是程序槽。

| 情况 | 给主 Agent | 上次 SQL + 查询摘要 |
|------|------------|---------------------|
| 有行 | SQL + 截断后的结果（最多 50 行或约 4000 字） | 成对覆盖 |
| 0 行 | 没查到 | 成对覆盖（0 行那条 SQL + 本轮需求说明） |
| 工具失败用尽 | 最后 SQL + 报错 | 旧值不动；本轮摘要扔掉 |

主 Agent 用人话回复。0 行就说没查到，不再自动改条件查一次。

完成：用户已经收到人话；两个槽要么成对覆盖，要么都没动。

---

## 两环对照

| | 环 A 正常对话 | 环 B 查数 |
|--|---------------|-----------|
| 何时进 | 规划认为不需要查库 | 规划认为需要查库，调查数工具一次 |
| 经过谁 | 只有主 Agent | 主 Agent 调一次查数工具（内部：小模型 → Vanna RAG → 查库对话）→ 回填主 Agent |
| 小模型 / Vanna RAG / 查库大模型 | 不出现 | 只出现在查数工具内部 |
| 查数工具 | 不调 | 调一次 |
| 联网搜索 | 默认关闭；开启后可以 | 本句不调 |
| 结束 | 回复用户，等下一句 | 回复用户，等下一句 |
| 主 Agent 记忆 | 会话 | 会话 |
| 小模型记忆 | — | 无跨轮记忆 |
| 查库大模型记忆 | — | 仅这一次查库对话；新 RAG 则全新 |

---

## 环 B 配置：只读库、Vanna RAG

### 1. 只读查库工具

程序提供给查库大模型的唯一工具。配的是连接和权限。

| 配什么 | 怎么定 |
|--------|--------|
| 地址 | host、端口、库名 |
| 账号 | 只读账号（只能 SELECT） |
| 执行 | 模型调工具，程序跑 SQL |
| 保险 | 禁写语句、表白名单、LIMIT、超时 30 秒、每段对话最多 5 次 |

```text
Vanna 配置
  ├── 查库大模型（生成 SQL，只调只读查库工具）
  └── 只读数据库连接
        host / port / database / user(只读) / password
```

生产用只读账号。报错原文返回给这段查库对话。

### 2. Vanna 自带 RAG

检索只用框架内 RAG。query 是改写问题。

| 字段 | 里面放什么 | 改写问题拿来对照什么 |
|------|------------|----------------------|
| **DDL / 表结构** | 建表语句、字段名、类型 | 这次可能用到哪些表、哪些列 |
| **文档 / 口径** | 指标含义、业务说明、同义词 | 「销售额」对应哪一列、怎么过滤 |
| **问题-SQL 样例** | 以前的完整问题 + 对应 SQL | 有没有相似问法可以参考 |

```text
改写问题
    → Vanna 内向量检索
        ├─ 对照「表结构」→ 命中相关 DDL
        ├─ 对照「文档口径」→ 命中相关说明
        └─ 对照「问题-SQL」→ 命中相似样例
    → 三路命中拼在一起
    → 交给查库对话（不再搜第二遍）
```

实现时走「改写句 → 框架 retrieve → 把 context 交给查库对话」。不要走默认 `generate_sql(问题)` 全链，那会再检索一次。

三类都要预先 train。缺哪一类，对照就会偏。

---

## 为什么这个循环能转起来：ReAct 思考模式

```text
思考（Reason）→ 行动（Act）→ 观察（Observe）→ 再思考 → ……
```

| ReAct | 在本系统里 |
|-------|------------|
| **观察** | 用户这句话；若刚查过数，还看到结果、没查到、或报错。联网搜索开启后，还可看到搜索结果 |
| **思考** | 主 Agent 规划：聊天还是查数？当前问题是否完整？ |
| **行动** | 环 A：直接说话（联网搜索开启后才可先搜再说话）。环 B：调查数工具一次 |
| **再观察** | 工具返回或用户下一句 → 再思考 |

- **环 A**：观察用户 → 思考「不用查库」→ 不调查数工具 → 说话 → 等下一句  
- **环 B**：观察用户 → 思考「要查库」→ 写出当前问题 → 调查数工具一次 → 观察回填 → 对人说 → 等下一句  

思考主体是主 Agent。查数工具是它启动环 B 的那一只手。小模型、Vanna RAG、查库大模型在工具内部，主 Agent 不能单独调。查库大模型在 B5 里可以多跑几次只读 SQL，那是查库对话，不是主 Agent 再调一次查数工具。联网搜索开启后，环 A 里可以多次搜索，仍不调查数工具。

---

## 已定数字

| 项 | 值 |
|----|----|
| 每段查库对话最多只读调用 | 5 |
| 单次 SQL 超时 | 30 秒 |
| 回填截断 | 50 行或约 4000 字 |
| 查数工具 | **默认开启**；每句最多 1 次 |
| 联网搜索 | **默认关闭** |
| 联网搜索开启后：每句环 A 最多调用 | 3 |
| 联网搜索开启后：每次最多条数 / 单条摘要 | 5 条 / 约 300 字 |
| 联网搜索开启后：单次超时 | 15 秒 |

表白名单具体表、Vanna 三类训练材料正文、多业务是否拆训练空间：落地时再填。
